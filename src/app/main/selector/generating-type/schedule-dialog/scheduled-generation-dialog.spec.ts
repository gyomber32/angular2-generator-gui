/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ScheduledGenerationDialog } from './scheduled-generation-dialog.component';

import { BrowserAnimationsModule, NoopAnimationsModule } from '@angular/platform-browser/animations';

import { MatInputModule, MatButtonModule, MatFormFieldModule, MatDialogModule, MatDialogRef } from '@angular/material';

import { OwlDateTimeModule, OwlNativeDateTimeModule } from 'ng-pick-datetime';

import { FormsModule } from '@angular/forms';

describe('ScheduledGenerationDialog', () => {
  let component: ScheduledGenerationDialog;
  let fixture: ComponentFixture<ScheduledGenerationDialog>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ScheduledGenerationDialog],
      imports: [
        BrowserAnimationsModule,
        NoopAnimationsModule,
        FormsModule,
        MatDialogModule,
        MatInputModule,
        MatButtonModule,
        MatFormFieldModule,
        OwlDateTimeModule,
        OwlNativeDateTimeModule],
      providers: [MatDialogRef]
    }).compileComponents().then(() => {
      fixture = TestBed.createComponent(ScheduledGenerationDialog);
      component = fixture.componentInstance;
      fixture.detectChanges();
    });
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */